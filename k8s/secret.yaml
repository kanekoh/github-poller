apiVersion: v1
kind: Secret
metadata:
  name: github-poller-secret
  namespace: default
type: Opaque
stringData:
  # GitHub Personal Access Token を設定してください
  # 必要なスコープ: repo（プライベートリポジトリの場合）、または public_repo（パブリックリポジトリのみの場合）
  github-token: "YOUR_GITHUB_TOKEN_HERE"

